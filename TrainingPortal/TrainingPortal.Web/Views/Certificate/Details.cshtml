@model TrainingPortal.Models.Certificate

@{
	ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
	<h4>Certificate</h4>
	<hr />
	<dl class="dl-horizontal">
		<dt>
			@Html.DisplayNameFor(model => model.UserId)
		</dt>

		<dd>
			@Html.DisplayFor(model => model.UserId)
		</dd>

		<dt>
			@Html.DisplayNameFor(model => model.Result)
		</dt>

		<dd>
			@Html.DisplayFor(model => model.Result)
		</dd>

		<dt>
		</dt>
		<dd>
			@Html.ActionLink("Для печати", "Index", "Certificate", new { courseId = Request.Params["courseId"] }, new { @class = "btn btn-primary", @style = "margin-top:20px;" })
		@Html.ActionLink("Удалить", "Delete", "Certificate", new { Id = Model.Id, courseId = Request.Params["courseId"] }, new { @class = "btn btn-primary", @style = "margin-top:20px;" })
		</dd>
	</dl>
	<div class="certificate col-md-offset-1">
		@Html.Partial("Partials/Index", Model)
	</div>
</div>
