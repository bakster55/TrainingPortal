@model TrainingPortal.Models.ApplicationUser

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>ApplicationUser</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.UserName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.UserName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Email)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Email)
        </dd>

			<hr />

			<dt>Password:</dt>
			<dd>
				[@Html.ActionLink("Change password", "ChangePassword", new { id = Model.Id })]
			</dd>

			<dt>Profile:</dt>
			<dd>
				[@Html.ActionLink("Change profile", "ChangeProfile", new { id = Model.Id })]
			</dd>

			<dt>Role:</dt>
			<dd>
				[@Html.ActionLink("Change role", "ChangeRole", new { id = Model.Id })]
			</dd>
    </dl>
</div>
<p>
    @Html.ActionLink("Back to List", "Index")
</p>
