@model TrainingPortal.Models.TestOption

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>TestOption</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.IsChecked)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.IsChecked)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id, testId = Request.Params["testId"] }) |
    @Html.ActionLink("Back to List", "Index", new { testId = Request.Params["testId"] })
</p>
